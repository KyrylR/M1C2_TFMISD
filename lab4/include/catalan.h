#ifndef CATALAN_H
#define CATALAN_H

#include <stdbool.h>

// Function prototype for calculating Catalan numbers
void printCatalanNumbers(const int n, const bool verbose);
void benchmarkCatalan(const int n, const bool verbose);

#endif // CATALAN_H
